{% extends "layoutUser.njk" %}

{% block content %}
    
    <!-- ===== MAIN ===== -->

    <main class="bg-main max-width">
        <div class="intro">
            <h1>As melhores receitas</h1>
            <p>Aprenda a construir os melhores pratos com receitas 
            criadas por profissionais do mundo inteiro.</p>
        </div>
        <img src="/assets/chef.png" alt="" srcset="">
    </main>
    <section class="max-width">

        <h2>Mais acessadas</h2>

    <!-- ===== PRODUCTS ===== -->        

        <div class="products">

            {% for item in items %}
            <div class="recipe">

                <img class="itemIndex" src="{{item.image}}" alt="Foto da receita {{item.title}}" id="{{ loop.index }}">

                <h3 class="itemIndex" id="{{ loop.index }}">{{item.title}}</h3>
                <p>{{item.author}}</p>
            </div>
            {% endfor %}

        </div>

    </section>
    <script src="/js/openPageUser.js"></script>
{% endblock %}